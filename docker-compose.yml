services:
  api:
    build: .
    ports:
      - "8080:8080"
    env_file: .env
    environment:
      - REDIS_URL=redis://:admin123@redis:6379
    depends_on:
      - redis
    deploy:
      resources:
        limits:
          memory: 4G
        reservations:
          memory: 2G
  redis:
    image: "redis:alpine"
    ports:
      - "6379:6379"
    command: ["redis-server", "--requirepass", "admin123"]
